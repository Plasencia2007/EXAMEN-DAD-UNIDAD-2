spring:
  application:
    name: ms-gestion-instructor
  # Toda la configuracion (puerto, datasource, JPA, Eureka) llega del Config Server
  config:
    import: "optional:configserver:http://localhost:8888"
