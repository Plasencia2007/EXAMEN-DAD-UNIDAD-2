spring:
  application:
    name: ms-gestion-taller
  # Toda la configuracion (puerto, datasource, JPA, Eureka, Feign) llega del Config Server
  config:
    import: "optional:configserver:http://localhost:8888"
