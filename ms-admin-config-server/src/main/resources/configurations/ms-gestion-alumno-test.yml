# ============================================================
# Perfil TEST de ms-gestion-alumno
# H2 en memoria (MODE=MySQL), ddl-auto=create-drop, show-sql=true.
# Requiere la dependencia H2 en el pom (scope runtime).
# ============================================================
spring:
  datasource:
    url: "jdbc:h2:mem:db_alumno;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE"
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
