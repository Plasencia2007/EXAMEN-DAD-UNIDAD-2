# ============================================================
# Perfil DEV de ms-seguridad
# MySQL local (puerto 3307), show-sql=true, ddl-auto=update.
# ============================================================
spring:
  datasource:
    url: "jdbc:mysql://localhost:3307/db_seguridad?createDatabaseIfNotExist=true&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false"
    username: root
    password: mysql123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
