spring:
  application:
    name: ms-seguridad
  # Toda la configuracion (puerto, datasource, JPA, Eureka, JWT) llega del Config Server
  config:
    import: "optional:configserver:http://localhost:8888"
